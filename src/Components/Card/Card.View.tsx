import React,{ FC } from 'react';
import * as CardStyles from './Card.styled';

const { Container } = CardStyles;
const Card:FC = ()=> {
  return (
    <Container>
      Erase una vez
    </Container>
  )
}

export default Card
