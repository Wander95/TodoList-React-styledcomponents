import { DefaultTheme } from 'styled-components'
import colors from './colors';

const Theme: DefaultTheme = {
  borderRadius: {
    light:'5px',
    medium:'10px',
    strong:'15px'
  },
  colors,
}

export { Theme }